1) Artnet data and playing internal chases should not override Highlite mode. So need to create bool var _highlite which will block writing to WS 
    when _highlite is setted (May be only for artnet data and playing effects, may be globally for all).
    1.1) _highlite should be setted when command S H received an unset when S h received.
    1.2) if _highlite is not unset by remote controller for some time (30s may be) then esp should unset _highlite itself for avoiding blocking of
        output due to some lags or unreceived commands

2) 